"""Infrastructure layer. SQLAlchemy repositories, license, external services.

Phase 1 adds the SQLAlchemy (PostgreSQL) repositories implementing the ports in
dukan.application. The only layer allowed to know PostgreSQL exists.
"""
